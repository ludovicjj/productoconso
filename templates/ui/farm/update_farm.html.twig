{% extends 'base.html.twig' %}

{% block body %}
    <h1>Update farm</h1>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_rest(form) }}
    <button type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}