{% extends 'base.html.twig' %}

{% block body %}
    <h1>Welcome registration page</h1>

    <div>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}